name: Release App

on:
  push:
    branches:
      - main

<<<<<<< HEAD
permissions:
  contents: write

=======
>>>>>>> ab237ae (feat: add sidebar navigation, automated release pipeline, and print styles for orders)
jobs:
  release:
    runs-on: windows-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

<<<<<<< HEAD
      - name: Get version from package.json
        id: get_version
        run: |
          $version = (Get-Content package.json | ConvertFrom-Json).version
          echo "VERSION=$version" >> $env:GITHUB_ENV
        shell: powershell

      - name: Pre-create GitHub Release Draft
        run: gh release create "v${{ env.VERSION }}" --draft --title "v${{ env.VERSION }}" --notes "Release v${{ env.VERSION }}" || echo "El borrador de la versión v${{ env.VERSION }} ya existe."
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

=======
>>>>>>> ab237ae (feat: add sidebar navigation, automated release pipeline, and print styles for orders)
      - name: Build and Publish Release
        run: npm run release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
